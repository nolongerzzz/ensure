// demo/app-different.js — exists only so a negative-control assetHash
// check has something guaranteed to hash-mismatch against the live
// demo/app.js. Not referenced by index.html.
window.state = { ready: true, placed: [], extra: 'this file is intentionally different' };
