{
  "checks": [
    {
      "id": "hud-version-agree",
      "kind": "agree",
      "sources": [
        { "label": "index-script-tag", "read": { "type": "attr", "selector": "script#main-script", "attr": "src", "extractParam": "v" } },
        { "label": "hud-stamp-text", "read": { "type": "text", "selector": "#adjust-status" } }
      ]
    },
    {
      "id": "prefixed-stamp-agree",
      "note": "Positive control for the extractPattern read option: #prefixed-stamp embeds the version in a longer line, so it only agrees with the script tag's ?v= if the pattern actually narrows the read. Trailing text after the version is tolerated on purpose — nest-optimizer appends a running count to that same line.",
      "kind": "agree",
      "sources": [
        { "label": "index-script-tag", "read": { "type": "attr", "selector": "script#main-script", "attr": "src", "extractParam": "v" } },
        { "label": "prefixed-stamp-text", "read": { "type": "text", "selector": "#prefixed-stamp", "extractPattern": "^HUD\\s+(\\S+)" } }
      ]
    },
    {
      "id": "global-state-exists",
      "kind": "globalExists",
      "path": "state",
      "expectType": "object"
    },
    {
      "id": "app-js-hash-matches-repo",
      "kind": "assetHash",
      "liveUrl": "app.js",
      "repoFile": "demo/app.js"
    },
    {
      "id": "app-js-parses",
      "kind": "parses",
      "liveUrl": "app.js",
      "scriptType": "classic"
    },
    {
      "id": "module-js-parses",
      "note": "A valid ES module must not read as a syntax error. Classic-script-only parsing rejects `export` with a message indistinguishable from a truncated file, which would make every module asset a permanent false red.",
      "kind": "parses",
      "liveUrl": "module.js",
      "scriptType": "module"
    }
  ]
}
